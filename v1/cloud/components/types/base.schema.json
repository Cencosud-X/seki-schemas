{
  "$schema": "http://json-schema.org/schema",
  "required": ["identifier", "kind", "settings", "outputs"],
  "properties": {
    "identifier": {
      "type": "string",
      "description": "Cloud component identifier",
      "default": "default"
    }
  }
}
