{
  "$schema": "http://json-schema.org/schema",
  "required": ["identifier", "kind", "settings"],
  "properties": {
    "identifier": {
      "type": "string",
      "description": "Cloud component identifier",
      "default": "default"
    }
  }
}
